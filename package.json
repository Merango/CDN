{
  "name": "cdn-file-server",
  "version": "1.0.0",
  "description": "Secure CDN file serving application",
  "main": "src/index.js",
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "vitest": "^1.0.0"
  }
}