{
  "name": "cdn-file-service",
  "version": "1.0.0",
  "description": "CDN file management service",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "latest",
    "typescript": "latest"
  }
}